"""habits — habit tracker (habits.kynguyen.cc).

Two tables: `habits` (definitions) and `habit_logs` (one check-in per
(habit, date)). Mirrors the journal app's user-scoped, JWT-cookie-auth pattern.
"""
